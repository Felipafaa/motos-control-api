
spring.application.name=motos-control-api
spring.datasource.url=jdbc:postgresql://pgmotoschallenge557636.postgres.database.azure.com:5432/motosdb
spring.datasource.username=pgadmin
spring.datasource.password=DesafioFiap@2025
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.flyway.baseline-on-migrate=true
app.admin.emails=admin@example.com
# --- OUTRAS CONFIGURAÇÕES (DESATIVADAS TEMPORARIAMENTE) ---
# Comentei as linhas abaixo para focarmos apenas no problema do banco de dados.
# Depois que a API funcionar, você pode reativá-las se precisar.
# spring.flyway.database-type=postgresql
# spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
# spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
# spring.security.oauth2.client.registration.google.scope=openid,email,profile
# spring.security.oauth2.client.registration.google.redirect-uri=http://localhost:8080/login/oauth2/code/google
# app.admin.emails=${APP_ADMIN_EMAILS}